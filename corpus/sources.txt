# One URL per line. Lines starting with # are comments.
# The miner fetches each URL and treats the body as an AI-prose sample.
# Truncated to 20KB per URL to bound token use.
#
# Example:
# https://example.com/blog/some-ai-generated-post
